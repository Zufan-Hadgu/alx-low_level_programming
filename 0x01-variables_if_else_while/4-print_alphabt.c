#include <stdio.h>
/**
 *main-Entry point
 *
 *Return: always 0(success)
 */
int main(void)
{
	char alf = 'a';

	while (alf <= 'z')
	{
		if (alf != 'e' && (alf != q))
		{
			putchar(alf);
		}
	}
	putchar('\n');
	return (0);
}
